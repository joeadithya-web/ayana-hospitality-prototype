import { ConciergeRequestList } from '../components/ConciergeRequestList';
import { IntentTaskList } from '../components/IntentTaskList';

export function ConciergeDesk() {
  return (
    <div className="flex flex-col gap-6">
      <ConciergeRequestList
        types={[
          'airport_pickup',
          'taxi',
          'restaurant_booking',
          'spa_booking',
          'local_recommendation',
          'wake_up_call',
          'special_request',
        ]}
        emptyLabel="Guest concierge requests from the Traveller App and Kiosk will appear here."
      />

      <section>
        <h2 className="mb-2 font-display text-base font-semibold text-ink-950">Journey Tasks</h2>
        <p className="mb-3 text-xs text-ink-700/50">
          Hotel-side tasks generated by a guest's declared travel Intent — e.g. holding a meeting room for a
          Business Presentation.
        </p>
        <IntentTaskList departments={['front_office', 'concierge']} />
      </section>
    </div>
  );
}
